# Smart Calc — find_power Audit

## 1. Objetivo

Este documento registra a auditoria técnica da função `find_power()` da Smart Calculator original.

A função `find_power()` é o coração da calculadora, pois busca o percentual ideal de força da tacada para que a bola alcance uma distância e altura específicas.

O objetivo desta auditoria é preparar a futura implementação em Python.

---

## 2. Funções que serão implementadas

No projeto Python, estas funções devem ficar em:

```text
src/pangya_physics/solver.py